//passing argument and parameter in functions

const speak = function(name,time){ //scoop

    console.log(`good day ${time} ${name}`);
}


speak('amine','morning');

/* when the function is called  because when the function is invoked

amine take the variable name that we can acces after in the function


*/
